class Patient {
  String? name;
  String? diagnostic;
  int? _ptCode;

  Patient(this.name, this.diagnostic);
  Patient._named(this.name, this.diagnostic);
  factory Patient(String? name, String? diagnostic) {
    if(){}
  }
}

void main() {}
